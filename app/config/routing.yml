#msg_parser:
#    resource: "@ParserBundle/Controller/"
#    type:     annotation
#    prefix:   /

app:
    resource: '@AppBundle/Controller/'
    type: annotation

parser:
    resource: '@ParserBundle/Controller/'
    type: annotation
