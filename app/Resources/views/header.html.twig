<header>
    <div class="container">
        <div class="row">
            <div class="col">
                <div class="alert alert-light">
                    <h1><a href="{{ path("homepage") }}">PHP School tasks</a></h1>
                </div>
            </div>
            <div class="col">
                <div class="alert alert-light">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') == false %}
                    <p class="text-md-right"> <a href="{{ path("auth") }}">Sign In</a></p>
                    {% else %}
                    <p class="text-md-right"> <a href="{{ path("logout") }}">Sign out</a></p>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</header>